import { defineFunction } from "@aws-amplify/backend";

/**
 * توليد رقم الطلب التسلسلي.
 *
 * الطريقة السابقة كانت: ولّد رقمًا عشوائيًا، اسأل الجدول هل هو موجود،
 * ثم أنشئ الطلب. بين السؤال والإنشاء نافذة بالميلي‌ثانية يستطيع موظف
 * آخر أن يحجز فيها الرقم نفسه — والفهرس الثانوي في DynamoDB ليس قيد
 * تفرّد فلا يمنع الثاني. النتيجة: أنبوبان بباركود واحد، ونتائج مريض
 * تُنسب إلى مريض آخر.
 *
 * الحل هنا عدّاد لكل يوم يُزاد بمقارنة-وتبديل (compare-and-swap): يقرأ
 * الدالة قيمته ثم تكتب القيمة التالية بشرط أن تكون القيمة الحالية لم
 * تتغيّر. من يسبق يفوز، ومن يخسر يعيد المحاولة بقيمة أحدث — فلا نافذة
 * سباق أصلًا لا نافذة ضيّقة.
 *
 * ولماذا في دالة لا في المتصفّح: العدّاد وحده مصدر الأرقام، فلا يجوز
 * أن يملك أي عميل صلاحية الكتابة عليه — عميل يضبطه على قيمة قديمة
 * يُعيد إصدار أرقام مستعملة.
 */
export const nextOrderNo = defineFunction({
  name: "next-order-no",
  entry: "./handler.ts",
  timeoutSeconds: 20,
});
