# نظام إدارة المختبر الطبي (LIS)

تطبيق Next.js + AWS Amplify Gen 2 يغطي رحلة العيّنة كاملة:

```
تسجيل المريض ← طلب فحص (رقم فريد) ← سحب العيّنة ← إدخال النتائج
   ← علَم تلقائي (طبيعي / مرتفع / منخفض / حرج) ← اعتماد ← تقرير قابل للطباعة
```

**الموقع المنشور:** <https://main.d1mvwoi85qfdt.amplifyapp.com>
(Amplify Hosting · منطقة `us-east-2` · نشر يدوي من مجلد `out/`؛ الباكند في
`us-east-1`). لتحديثه: `npm run build` ثم ارفع محتوى `out/` عبر
`aws amplify create-deployment` — أو اربط المستودع بالفرع `main` من لوحة
Amplify ليبني تلقائيًا عند كل دفع.

## التشغيل

```bash
npm install
npx ampx sandbox        # ينشئ الباكند على AWS ويولّد amplify_outputs.json
npm run dev             # http://localhost:3000
```

> ملف `amplify_outputs.json` الموجود حاليًا **مؤقت** — أمر `ampx sandbox` يستبدله
> بالقيم الحقيقية. قبل تشغيل الـ sandbox لن تعمل عمليات القراءة والكتابة.
> يتطلب `ampx sandbox` بيانات اعتماد AWS مضبوطة (`aws configure`).

## بيئة التقييم مقابل بيئة الإنتاج

`ampx sandbox` بيئة **مؤقتة** مرتبطة بجهازك ومستخدمك: لا نسخ احتياطي عليها،
و`npx ampx sandbox delete` يمحو جداول DynamoDB بما فيها من نتائج مرضى بلا
استرجاع. استخدمها للتقييم والتدريب فقط.

للإنتاج:

```powershell
.\deploy.ps1 -Production   # يطبع الخطوات كاملة
```

باختصار: ادفع إلى GitHub ← أنشئ تطبيقًا في Amplify Hosting على الفرع `main`
(ملف [amplify.yml](amplify.yml) يبني الباكند والفرونت معًا) ← فعّل
Point-in-Time Recovery على جداول DynamoDB ← فعّل termination protection على
المكدّس. وتحقّق من متطلبات توطين البيانات الصحية في بلدك قبل اختيار المنطقة.

## بوابة الزائر (المريض) — بلا تسجيل دخول

التطبيق يُفتح بلا شاشة دخول، لكن **الزائر يبحث عن تقريره وحده**:

1. يُدخل **رقم الطلب** (المطبوع على الإيصال والأنبوب) **مع رقم الملف أو الجوال**
   — رقمان معًا لأن رقم الطلب وحده يسهل تخمينه.
2. لا يُعرض التقرير قبل **الاعتماد** — نتيجة غير معتمدة قد تتغيّر بعد مراجعة
   مسؤول الجودة. قبل ذلك تظهر له حالة الطلب فقط.
3. رسالة الفشل واحدة لعدم وجود الطلب ولعدم تطابق الرقم الثاني، حتى لا يُستدلّ
   على أرقام الطلبات الموجودة.

**الزائر لا يملك أي صلاحية على الجداول** — لا قراءة ولا كتابة. منفذه الوحيد
استعلام مخصّص واحد `getMyReport` تنفّذه دالة Lambda
([amplify/functions/get-my-report](amplify/functions/get-my-report/handler.ts)):
هي التي تقرأ الجداول (بصلاحية `allow.resource`) بعد أن تتحقّق من الرقمين، ولا
تُرجع إلا تقريرًا معتمدًا يخصّ صاحبهما، وببيانات مريض مقتصرة على ما يُطبع
(اسم · رقم ملف · جنس · تاريخ ميلاد — بلا عنوان ولا هوية ولا ملاحظات سريرية).

هذا يغلق ثغرة أساسية: صلاحيات AppSync على مستوى الجدول لا الصفّ، فلو مُنح
الزائر `read` على `Order` لاستطاع من يستدعي الـ API مباشرةً — خارج الواجهة —
قراءة طلبات الناس جميعًا.

كل صفحات الموظفين محجوبة عن الزائر في نقطة واحدة (`Gate` في
[app/providers.tsx](app/providers.tsx)) — فأي صفحة تُضاف لاحقًا تكون محجوبة
افتراضيًا.

تسجيل الدخول للموظفين من `/login`؛ المسجَّل يمرّ عبر `userPool` فتبقى قواعد
المجموعات (الأدوار) سارية عليه.

## الخطوة الأولى بعد التشغيل

1. (اختياري — للعمل بالأدوار) أنشئ حسابًا من `/login`.
2. أضف حسابك إلى مجموعة `admin` (المجموعات لا تُمنح تلقائيًا):

```bash
# رقم الـ User Pool موجود في amplify_outputs.json ← auth.user_pool_id
aws cognito-idp admin-add-user-to-group \
  --user-pool-id <USER_POOL_ID> \
  --username <بريدك@example.com> \
  --group-name admin
```

   هذا الأمر لازم **لأول مدير فقط**؛ بعده تُدار المجموعات كلها من صفحة
   **الموظفون** (`/staff`).

3. سجّل خروجًا ودخولًا (ليُحدَّث التوكن)، ثم افتح **كتالوج الفحوصات** واضغط
   «تحميل الكتالوج الافتراضي» — يضيف ٤٠ فحصًا شائعًا بمدياتها المرجعية.
4. افتح **الإعدادات** واضبط اسم المختبر والعملة وقوائم العيّنات والأنابيب.
5. سجّل مريضًا ← أنشئ طلب فحص ← أدخل النتائج ← اعتمدها ← اطبع التقرير.

## ما يديره مدير المختبر

| أين | ماذا |
| --- | --- |
| **الإعدادات** (`/settings`) | اسم المختبر · العملة (رمزها وكودها) · قائمة أنواع العيّنات · قائمة أنواع الأنابيب (التيوبات) · **صيانة البيانات**: ملء الحقل `day` رجعيًّا |
| **كتالوج الفحوصات** (`/catalog`) | إضافة فحص جديد · تعديل الاسم العربي والإنجليزي والكود · السعر (تحرير مباشر في الجدول) · الوحدة · نوع العيّنة والأنبوب · **المديات المرجعية** (حسب الجنس والعمر) · الحدود الحرجة · الترتيب · تفعيل/إيقاف |
| **الموظفون** (`/staff`) | قائمة المستخدمين وأدوارهم · منح دور وسحبه · تعطيل حساب وتفعيله · إرسال رمز إعادة تعيين كلمة المرور |
| **التقارير** (`/reports`) | الإيراد والمحصَّل والمتبقّي على فترة · عدد الطلبات ومتوسط الفاتورة · زمن الإنجاز (متوسط ووسيط) · نسبة الإلغاء · أكثر الفحوصات طلبًا |
| **سجل التدقيق** (`/audit`) | كل حدث في يوم مختار، مع بحث بالفاعل أو رقم الطلب أو نص الحدث |

- العملة تُحفظ في `LabConfig` وتُقرأ مرة واحدة عند فتح التطبيق، فتظهر في كل
  الأسعار والفواتير والتقارير. **تغيير الرمز لا يحوّل المبالغ المحفوظة.**
- قوائم العيّنات والأنابيب تظهر كاقتراحات (`datalist`) عند تعريف الفحص، ويبقى
  الحقل حرًّا لكتابة نوع خارج القائمة.
- كل تغيير سعر أو تعديل فحص يُكتب في `AuditLog`
  (`PRICE_CHANGED` · `TEST_UPDATED` · `TEST_CREATED`) مع القيمة قبل وبعد.

## الإيصال وملصقات الأنابيب

من صفحة الطلب زرّ **«الإيصال والملصقات»** (`/orders/receipt`) يطبع ورقتين من
مصدر واحد:

- **إيصال المريض** — رقم الطلب مطبوعًا وباركودًا، وتفصيل الفحوصات والمبلغ
  والمتبقّي. هو الورقة التي يعود بها المريض ليستعلم عن نتيجته.
- **ملصقات الأنابيب** — ملصق لكل أنبوب مطلوب لا لكل فحص: الفحوصات تُجمَّع
  حسب نوع الأنبوب، فثلاثة فحوصات كيمياء = ملصق واحد. على الملصق الباركود
  واسم المريض ونوع الأنبوب.

الباركود **Code 39** مولَّد في [lib/barcode.ts](lib/barcode.ts) بلا حزمة
خارجية ويُرسم SVG. اختير على Code 128 لأن جدوله ٤٤ حرفًا كل حرف مستقل بلا
حالات، فيقبل التحقّق البنيوي بالاختبارات؛ وخطأ ترميز في Code 128 يعطي
باركودًا يُمسح بنجاح ويعطي رقمًا آخر — أي عيّنة تُنسب إلى مريض غير صاحبها.

الطباعتان منفصلتان عمدًا: الإيصال على A4 أو ورق حراري، والملصقات على ورق
ملصقات؛ طباعتهما معًا تفسد إحداهما.

## قراءة الباركود بالماسح

الماسحات الشائعة في المختبرات تعمل كـ**إسفين لوحة مفاتيح**: لا تحتاج تعريفًا
ولا إذن متصفّح، بل تطبع أحرف الرمز ثم Enter. لذلك الالتقاط في
[lib/scanner.ts](lib/scanner.ts) من أحداث لوحة المفاتيح لا من كاميرا.

- **في [قائمة العمل](app/orders/page.tsx):** مسح أنبوب يفتح طلبه مباشرة —
  استعلام على فهرس `orderNo` لا بحث في المعروض، فالأنبوب الذي في يد الفنّي
  قد يكون خارج التصنيف المفتوح على الشاشة.
- **في [صفحة الطلب](app/orders/view/page.tsx):** بطاقة «مطابقة الأنبوب».
  هذا الحاجز الأخير قبل نسبة نتيجة إلى مريض خطأ: الفنّي يفتح طلبًا ويحمل
  أنبوبًا، ولا شيء يربط بينهما إلا انتباهه — وأنبوبان متجاوران على الحامل
  يكفيان. المطابقة تُسجَّل `TUBE_VERIFIED` وعدمُها `TUBE_MISMATCH`، وعدم
  المطابقة في قائمة الأحداث الجديرة بالانتباه: «كاد أن يقع» (near miss) هو
  ما تبحث عنه مراجعة الجودة.

المسح **اختياري ولا يمنع الحفظ**: مختبر بلا ماسح يجب أن يعمل، ومنعُ الحفظ
على ملصق تالف يوقف العمل. وفي البطاقة حقل كتابة يدوية للسبب نفسه.

تمييز المسح عن الكتابة البشرية بالسرعة: الماسح يرسل الأحرف بفاصل أقل من
٤٥ مللي‌ثانية، وأي حرف خارج أبجدية Code 39 **يُبطل الدفعة كلها** حتى تبدأ
دفعة جديدة — لا يُتخطّى وحده، وإلا تراكمت بقيّة أحرف مسح مشوَّش في رمز قصير
صحيح الشكل يخصّ طلبًا آخر موجودًا فعلًا.

> الالتقاط العام معطَّل حين يكون التركيز داخل حقل كتابة: شاشة إدخال النتائج
> مليئة بحقول أرقام، والتقاط ما يُكتب فيها يغيّر نتيجة مريض بصمت.

## ترقيم الطلبات

رقم الطلب (`LAB-260729-000042`) يحجزه عدّاد تسلسلي لكل يوم في دالة
[amplify/functions/next-order-no](amplify/functions/next-order-no/handler.ts)
بـ**مقارنة-وتبديل** ذرّية: تقرأ الدالة قيمة العدّاد ثم تكتب التالية بشرط أن
تكون الحالية لم تتغيّر (`condition: { value: { eq: current } }`)، فمن يسبق
يفوز ومن يخسر يعيد المحاولة بقيمة أحدث.

الطريقة السابقة كانت «ولّد عشوائيًا ثم اسأل هل الرقم موجود» — وبين السؤال
والإنشاء نافذة بالميلي‌ثانية يحجز فيها موظف آخر الرقم نفسه، والفهرس الثانوي
في DynamoDB ليس قيد تفرّد فلا يمنعه. النتيجة أنبوبان بباركود واحد.

- نموذج `Counter` **لا يكتب عليه أي عميل** — من يكتب عليه يعيد إصدار أرقام
  مستعملة. القراءة لمجموعة `admin` وحدها، والكتابة للدالة عبر
  `allow.resource(nextOrderNo).to(["query", "mutate"])`.
- **ختم اليوم يرسله العميل** (`stamp` = YYMMDD): Lambda تعمل بتوقيت UTC
  والمختبر بتوقيته، ولو حُسب في الدالة لبدأ عدّاد «اليوم» عند الثالثة صباحًا
  بالرياض فحمل طلبان في ليلة واحدة ختمَي يومين.
- **الرجوع إلى التوليد العشوائي** عند تعذّر الدالة مقصود: عطل في العدّاد
  يبطئ المختبر ولا يوقف استقبال المرضى. والرقمان بالطول نفسه فلا يتغيّر
  عرض الباركود.

## الدفعات

الفاتورة في صفحة الطلب تُظهر الصافي والمدفوع و**المتبقّي**، وزرّ «تسجيل
دفعة» لمن يملك `viewFinance`. الدفعة تُسجَّل في `AuditLog` كـ
`PAYMENT_RECORDED` مع المبلغ قبل وبعد، ولا تُقبل زيادةً على المتبقّي.

> قيد معروف: الدفعة قراءة‑ثم‑كتابة على `paidAmount` بلا قفل — دفعتان في
> اللحظة نفسها على الطلب نفسه تُضيّع إحداهما. المخرج الكامل جدول `Payment`
> مستقل تُجمع سطوره، ويلزم عند تعدّد نقاط التحصيل.

## إدارة الموظفين والأدوار

الأدوار مجموعات في Cognito لا صفوف في جدول، فلا تُدار من الواجهة إلا عبر
دالة تملك صلاحية استدعاء Cognito:
[amplify/functions/manage-staff](amplify/functions/manage-staff/handler.ts).
الاستعلام والطفرة محصوران بمجموعة `admin` في المخطط، فيرفض AppSync الطلب
قبل أن يبلغ الدالة.

- صلاحيات الدالة مذكورة واحدة واحدة — لا `cognito-idp:*` — ومحصورة في
  مجمّع مستخدمي هذا التطبيق وحده.
- **لا تشمل `AdminSetUserPassword` ولا `AdminInitiateAuth`:** الأولى تتيح
  للمدير ضبط كلمة موظف ثم الدخول باسمه، والثانية انتحال جلسة. إعادة
  التعيين ترسل رمزًا إلى بريد الموظف نفسه، فيبقى سجل التدقيق دالًّا على
  فاعله الحقيقي.
- **حارس القفل الذاتي:** المدير لا يزيل صفة `admin` عن نفسه ولا يعطّل
  حسابه — لو لم يبقَ مدير آخر لضاع النظام على المختبر ولم تُستردّ الإدارة
  إلا من سطر أوامر AWS.

هذا يُغني عن `aws cognito-idp admin-add-user-to-group` بعد أول مدير.

## تقارير الإدارة وسجل التدقيق

الصفحتان تقرآن عبر فهرس `day` لا بمسح الجداول: تقرير شهر يقرأ طلبات ذلك
الشهر وحدها مهما كبر تاريخ المختبر، وسجل التدقيق — وهو أسرع الجداول نموًّا
— يُقرأ يومًا بيوم. الحقل `day` بصيغة `YYYY-MM-DD` **بالتوقيت المحلي لا
UTC**: تقرير «أمس» يجب أن يطابق يوم المحاسب لا يوم غرينتش.

- حدّ فترة التقرير ٩٢ يومًا: كل يوم استعلام مستقل. التقارير السنوية تحتاج
  تجميعًا مسبقًا (جدول ملخّص يومي تكتبه دالة مجدولة) لا استعلامًا حيًّا.
- «أكثر الفحوصات طلبًا» بزرّ منفصل لأنه استعلام لكل طلب، ويُنفَّذ على دفعات.
- داخل صفحة الطلب لوحة **«سجل هذا الطلب»** تقرأ عبر فهرس `entityId`: أحداث
  الطلب تحت معرّفه، وأحداث كل نتيجة تحت معرّف فحصها، فتُجمع في خط زمني
  واحد يجيب «من غيّر هذه النتيجة ومتى» بلا فتح سجل اليوم كله.
- **الطلبات المنشأة قبل إضافة الحقل `day` لا تظهر في التقارير** حتى تُملأ
  رجعيًّا من «صيانة البيانات» في صفحة الإعدادات (انظر أدناه).

## صيانة البيانات

في [صفحة الإعدادات](app/settings/page.tsx) بطاقة **«صيانة البيانات»** لمدير
المختبر: تعدّ الطلبات التي لا تحمل الحقل `day` ثم تملؤه من `createdAt`.

الحقل أُضيف بعد أن كان النظام يعمل، والتقارير تقرأ عبر فهرسه — فالطلبات
الأقدم منه غائبة عن كل تقرير فترة: إيرادها لا يُحتسب وعدد فحوصها لا يظهر.
لا خطأ يُعرض، الأرقام تنقص بصمت وحسب.

- اليوم يُستخرج **بتوقيت المتصفّح**، وهو ما يفعله إنشاء الطلب أصلًا فيتطابق
  القديم مع الجديد. لذلك تُشغَّل من جهاز على توقيت المختبر.
- طلب بطابع زمني مفقود أو تالف **يُترك بلا يوم** ولا يُوضع في يوم مخترَع:
  طلب في اليوم الخطأ يشوّه تقرير تلك الفترة أكثر من طلب خارج التقرير.
- العملية قابلة لإعادة التشغيل: تُحدّث الناقص وحده، وتقول «لا حاجة لشيء»
  بعد اكتمالها. وتُسجَّل مرة واحدة في `AuditLog` كـ `DAY_BACKFILLED`.

## الأدوار

| المجموعة    | الصلاحية                                                     |
| ----------- | ------------------------------------------------------------ |
| `admin`     | كل شيء + إدارة الكتالوج والإعدادات والموظفين                  |
| `quality`   | مراجعة واعتماد النتائج وتعديل تقرير معتمد + التقارير والتدقيق |
| `tech`      | إدخال النتائج                                                 |
| `reception` | تسجيل المرضى والطلبات والفواتير والدفعات                      |
| `doctor`    | قراءة فقط                                                     |

سجل التدقيق أداة مراجعة، فهو لمن يراجع (`admin` · `quality`) لا لمن
يُراجَع عمله. والتقارير تكشف الإيراد، فهي لمن يراه أصلًا. وتبويبات الشريط
العلوي تُخفى عمّن لا يملك إجراءها: تبويب يقود إلى صفحة ترفضه يعلّم الموظف
أن يتجاهل رسائل المنع بدل أن يقرأها.

الصلاحيات مطبَّقة في طبقتين: قواعد `authorization` في
[amplify/data/resource.ts](amplify/data/resource.ts) (التطبيق الفعلي على الخادم)،
ومصفوفة `MATRIX` في [lib/amplify.ts](lib/amplify.ts) (إخفاء الأزرار في الواجهة).

مستخدم بلا مجموعة لا يرى شيئًا وتظهر له لافتة تطلب منه مراجعة المدير — لا
يُعامَل كـ«استقبال» افتراضيًا.

**مبدأ أربع أعين:** من أدخل النتيجة لا يستطيع اعتمادها بنفسه. المدير (`admin`)
وحده يستطيع تجاوز ذلك، ويُسجَّل التجاوز صراحةً في سجل التدقيق.

## قرارات تصميمية مهمة

- **تجميد بيانات الفحص وقت الطلب:** `OrderItem` ينسخ الاسم والوحدة والمدى
  المرجعي والسعر من الكتالوج. تعديل الكتالوج لاحقًا لا يغيّر التقارير القديمة.
- **المدى المرجعي حسب المريض:** يُختار وقت إنشاء الطلب من `ranges` حسب جنس
  المريض وعمره (مثال: الهيموغلوبين والكرياتينين والفيريتين).
  **العمر أعلى أولوية من الجنس** في `pickRange`: الفروق بين طفل وبالغ أكبر من
  الفروق بين ذكر وأنثى، ولا تظهر فروق الجنسين أصلًا قبل البلوغ.
- **القيم الحرجة:** لكل فحص `criticalLow/criticalHigh`. عند تجاوزها يظهر تنبيه
  أحمر في الطلب وفي لوحة اليوم، ويُسجَّل اسم من أُبلغ ووقت الإبلاغ. التنبيه
  يعتمد على فهرس متفرّق (`criticalPending`) لا على مسح الجدول، ولا يسقط بعد
  انتهاء اليوم — قيمة حرجة لم يُبلَّغ عنها تبقى ظاهرة ومعلَّمة «متأخّرة».
- **سجل التدقيق:** كل إدخال/تعديل/اعتماد يُكتب في `AuditLog` مع القيمة قبل وبعد.
  التعديل بعد الاعتماد يُسجَّل كـ `RESULT_AMENDED`. **لا أحد — بما في ذلك
  `admin` — يملك `update` أو `delete` على هذا النموذج.**
- **تقرير مُصحَّح:** تعديل نتائج بعد الاعتماد يتطلّب سببًا مكتوبًا، ويرفع
  `reportRevision`، ويطبع على التقرير لافتة «مراجعة N — يُلغي ويستبدل أي نسخة
  سابقة» مع سبب التصحيح واسم من عدّله.
- **أرقام فريدة مؤكَّدة:** رقم الطلب (الباركود على الأنبوب) ورقم الملف يُفحص
  وجودهما عبر الفهرس الثانوي قبل الإنشاء ويُعاد التوليد عند التصادم — الفهرس
  الثانوي في DynamoDB ليس قيد تفرّد.
- **استعلامات مفهرسة لا مسح جداول:** فحوصات الطلب تُقرأ عبر
  `listOrderItemsByOrder`، وكل القوائم تتبع `nextToken` حتى النهاية
  (`listAll`) — لأن DynamoDB يرجع صفحة 1MB كحد أقصى ويتجاهل الباقي بصمت.
- **لا مفاتيح API عامة:** وضع المصادقة الافتراضي `userPool`، والزائر يمرّ عبر
  دور IAM مؤقّت من Identity Pool — لا مفتاح ثابت يُسرَّب أو ينتهي صلاحيته.
- **العملة وأنواع العيّنات والأنابيب في البيانات لا في الكود:** سجل `LabConfig`
  المفرد. إضافة نوع أنبوب أو تغيير العملة لا يستلزمان نشر إصدار جديد.
- **العيّنة والأنبوب حقلان منفصلان:** «دم وريدي» يُقال للمريض، و«أنبوب بنفسجي
  EDTA» تعليمة لفنّي السحب. دمجهما في نص واحد كان يمنع الفرز حسب الأنبوب.
- **رمز التحقق على التقرير:** بصمة مشتقّة من رقم الطلب ورقم الملف ووقت
  الاعتماد ورقم المراجعة وكل قيمة نتيجة. تغيير أي رقم يغيّر الرمز.
  هو كاشف تلاعب/خطأ نسخ وليس توقيعًا رقميًا (يحتاج مفتاح خادم).

## بنية المشروع

```
amplify/
  auth/resource.ts        المصادقة والمجموعات
  data/resource.ts        نماذج البيانات وقواعد الصلاحيات
  functions/get-my-report/ بوابة تقرير المريض (تحقّق من الرقمين)
  functions/manage-staff/  إدارة مستخدمي Cognito وأدوارهم
  functions/next-order-no/ حجز رقم الطلب التالي بعدّاد ذرّي
app/
  providers.tsx           حجب صفحات الموظفين عن الزائر (نقطة واحدة)
  page.tsx                لوحة اليوم للموظف · البحث عن التقرير للزائر
  components/ReportLookup.tsx  بوابة المريض والترحيب
  components/Barcode.tsx  رسم باركود Code 39 بـ SVG
  components/ScanBox.tsx  مسح باركود الأنبوب (ماسح أو كتابة يدوية)
  login/                  تسجيل دخول الموظفين (اختياري)
  patients/               تسجيل المرضى والبحث
  orders/                 قائمة العمل
  orders/new/             إنشاء طلب (اختيار مريض + فحوصات + فاتورة)
  orders/view/            إدخال النتائج والاعتماد وتسجيل الدفعات
  orders/receipt/         إيصال المريض وملصقات الأنابيب
  orders/report/          التقرير المطبوع (طباعة ← PDF)
  catalog/                كتالوج الفحوصات وتحريره
  settings/               إعدادات المختبر (عملة · عيّنات · أنابيب) + صيانة البيانات
  staff/                  الموظفون وأدوارهم
  reports/                تقارير الإيراد والإنتاجية والجودة
  audit/                  عرض سجل التدقيق يومًا بيوم
lib/
  lab.ts                  منطق المديات والأعلام والتنسيق والعملة والتواريخ
  amplify.ts              عميل البيانات، الجلسة والأدوار، سجل التدقيق
  barcode.ts              ترميز Code 39
  scanner.ts              قراءة الماسح ومطابقة الأنبوب بالطلب
  config.tsx              إعدادات المختبر المشتركة
  seedTests.ts            ٤٠ فحصًا افتراضيًا
  lab.test.ts             ٦٥ اختبار انحدار (`npm test`)
```

`npm test` يشغّل اختبارات المديات المرجعية والأعلام والتنسيق وترميز
الباركود وقراءته والتواريخ. و[.github/workflows/ci.yml](.github/workflows/ci.yml)
يشغّل فحص الأنواع والاختبارات والبناء عند كل دفع — بلا لمس AWS.

## دورة حياة الطلب

```
REGISTERED ──(سحب العيّنة)──> COLLECTED ──(إدخال نتائج)──> IN_PROGRESS
   └──> PENDING_REVIEW ──(اعتماد)──> APPROVED ──(تسليم)──> DELIVERED
   └──(إلغاء بسبب مكتوب، قبل الاعتماد)──> CANCELLED
```

الإلغاء والتسليم كلاهما يتطلّب مدخلًا من المستخدم (سبب الإلغاء / اسم المستلم)
ويُسجَّل في `AuditLog`. الطلبات الملغاة تُستثنى من عدّاد اليوم ومن الإيراد.

## المرحلة التالية (غير منفّذة)

- **جدول `Payment` مستقل** بدل تعديل `paidAmount` مباشرة — يلزم عند تعدّد نقاط التحصيل.
- **ملخّص يومي مجدول** ليصير التقرير السنوي ممكنًا بلا ٣٦٥ استعلامًا.
- توليد PDF على الخادم (Amplify Function) بدل طباعة المتصفح، وإرساله للمريض برابط مؤقت.
- ربط أجهزة التحليل عبر HL7 / ASTM.
- تقارير الجودة لكل قسم: زمن الإنجاز حسب القسم، ومعدل رفض العيّنات.

## تنبيه

المديات المرجعية في `lib/seedTests.ts` تقريبية وللعرض التوضيحي. قبل أي استخدام
حقيقي يجب أن يعتمدها مسؤول الجودة وفق الأجهزة والكواشف المستخدمة في المختبر.
